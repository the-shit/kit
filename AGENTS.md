# Kit

Loki-native factory agent. Sibling to Lexi, not a citizen in her process.

- SDK: `laravel/ai` via `App\Agent\BaseAgent` (pattern from `the-shit/agent-skeleton`).
- Routes: `/health`, `/api/ask`, `/api/webhooks/mattermost`. No Filament, no Horizon.
- Hands stay in `~/projects/bikes-v2` (TypeScript, Blender, Playwright).
- Catalog ids only. Never invent a gameplay GLB URL.
- Files under ~300 lines.

Ship gate: `php artisan test`.
